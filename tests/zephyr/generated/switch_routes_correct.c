/* Auto-generated by oz_static -- do not edit */
#include "oz_static_dispatch.h"
#include "switch_routes_correct.h"
#include "OZObject.h"

/* already resolved: #import "OZTestBase.h" */

/* @protocol Togglable -- compile-time only, see oz_static_dispatch.h/.c */

/* synthesized: backing storage for every LightSwitch instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_LightSwitch, sizeof(struct LightSwitch), 4, 4);

/* synthesized: allocates and zero-initializes a new LightSwitch (not from source) */
struct LightSwitch *LightSwitch_oz_alloc(void)
{
	struct LightSwitch *obj;
	if (oz_slab_alloc(&oz_slab_LightSwitch, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "LightSwitch pool exhausted -- raise it with --pool-sizes LightSwitch=N or an oz-pool comment");
#endif
		return (struct LightSwitch *)0;
	}
	memset(obj, 0, sizeof(struct LightSwitch));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_LightSwitch;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns LightSwitch's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void LightSwitch_oz_free(struct LightSwitch *obj)
{
	oz_slab_free(&oz_slab_LightSwitch, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation LightSwitch
 * -------------------------------------------------------------------------- */

/* - (int)toggle */
int LightSwitch_toggle(struct LightSwitch *self)
{
	/* _state = !_state; */
	self->_state = !self->_state;
	/* return _state; */
	return self->_state;
}

/*--------------------- end implementation: LightSwitch ----------------------*/


/* synthesized: backing storage for every Fan instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Fan, sizeof(struct Fan), 4, 4);

/* synthesized: allocates and zero-initializes a new Fan (not from source) */
struct Fan *Fan_oz_alloc(void)
{
	struct Fan *obj;
	if (oz_slab_alloc(&oz_slab_Fan, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Fan pool exhausted -- raise it with --pool-sizes Fan=N or an oz-pool comment");
#endif
		return (struct Fan *)0;
	}
	memset(obj, 0, sizeof(struct Fan));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Fan;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Fan's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Fan_oz_free(struct Fan *obj)
{
	oz_slab_free(&oz_slab_Fan, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Fan
 * -------------------------------------------------------------------------- */

/* - (int)toggle */
int Fan_toggle(struct Fan *self)
{
	/* _running = !_running; */
	self->_running = !self->_running;
	/* return _running + 10; */
	return self->_running + 10;
}

/*------------------------- end implementation: Fan --------------------------*/

