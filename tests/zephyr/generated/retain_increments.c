/* Auto-generated by oz2c -- do not edit */
#include "oz2c_dispatch.h"
#include "retain_increments.h"
#include "OZObject.h"

/* already resolved: #import "OZTestBase.h" */

/* synthesized: backing storage for every Node instance -- 4 slot(s), sized from this translation unit's allocation sites, each counted once per call site of the body it escapes from (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Node, sizeof(struct Node), 4, 4);

/* synthesized: allocates and zero-initializes a new Node (not from source) */
struct Node *Node_oz_alloc(void)
{
	struct Node *obj;
	if (oz_slab_alloc(&oz_slab_Node, (void **)&obj) != 0) {
#ifdef OZ_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Node pool exhausted -- raise it with --pool-sizes Node=N or an oz-pool comment");
#endif
		return (struct Node *)0;
	}
	memset(obj, 0, sizeof(struct Node));
	((struct OZObject *)obj)->_meta.class_id = OZ_CLASS_Node;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Node's slot to its slab -- called only from
 * oz_release, once the refcount reaches zero (not from source) */
void Node_oz_free(struct Node *obj)
{
#ifdef OZ_DEBUG_REFCOUNT
	/* Poison the slot on the way out (#452, #490). Read the comment
	 * on `render_freed_poison` before trusting any of this to be
	 * legible afterwards: both allocators write their free-list
	 * link over `_meta`, so the class_id stamp is gone the moment
	 * the slot goes back. The refcount sentinel is the one that
	 * survives -- it sits at sizeof(char *), just past the link --
	 * and oz_retain/oz_release check for it first. */
	((struct OZObject *)obj)->_meta.class_id = OZ_CLASS_ID_FREED;
	((struct OZObject *)obj)->_meta.immortal = 0;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, OZ_REFCOUNT_FREED);
	memset((char *)obj + sizeof(struct OZObject), 0xA5,
	       sizeof(struct Node) - sizeof(struct OZObject));
#endif
	oz_slab_free(&oz_slab_Node, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Node
 * -------------------------------------------------------------------------- */

/*------------------------- end implementation: Node -------------------------*/

