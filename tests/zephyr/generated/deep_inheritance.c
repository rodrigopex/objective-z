/* Auto-generated by oz_static -- do not edit */
#include "oz_static_dispatch.h"
#include "deep_inheritance.h"
#include "OZObject.h"

/* already resolved: #import "OZTestBase.h" */

/* synthesized: backing storage for every Level1 instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Level1, sizeof(struct Level1), 4, 4);

/* synthesized: allocates and zero-initializes a new Level1 (not from source) */
struct Level1 *Level1_oz_alloc(void)
{
	struct Level1 *obj;
	if (oz_slab_alloc(&oz_slab_Level1, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Level1 pool exhausted -- raise it with --pool-sizes Level1=N or an oz-pool comment");
#endif
		return (struct Level1 *)0;
	}
	memset(obj, 0, sizeof(struct Level1));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Level1;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Level1's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Level1_oz_free(struct Level1 *obj)
{
	oz_slab_free(&oz_slab_Level1, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Level1
 * -------------------------------------------------------------------------- */

/* - (int)depth */
int Level1_depth(struct Level1 *self)
{
	(void)self;
	return 1; }

/*------------------------ end implementation: Level1 ------------------------*/


/* synthesized: backing storage for every Level2 instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Level2, sizeof(struct Level2), 4, 4);

/* synthesized: allocates and zero-initializes a new Level2 (not from source) */
struct Level2 *Level2_oz_alloc(void)
{
	struct Level2 *obj;
	if (oz_slab_alloc(&oz_slab_Level2, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Level2 pool exhausted -- raise it with --pool-sizes Level2=N or an oz-pool comment");
#endif
		return (struct Level2 *)0;
	}
	memset(obj, 0, sizeof(struct Level2));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Level2;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Level2's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Level2_oz_free(struct Level2 *obj)
{
	oz_slab_free(&oz_slab_Level2, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Level2
 * -------------------------------------------------------------------------- */

/* - (int)depth */
int Level2_depth(struct Level2 *self)
{
	(void)self;
	return 2; }

/*------------------------ end implementation: Level2 ------------------------*/


/* synthesized: backing storage for every Level3 instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Level3, sizeof(struct Level3), 4, 4);

/* synthesized: allocates and zero-initializes a new Level3 (not from source) */
struct Level3 *Level3_oz_alloc(void)
{
	struct Level3 *obj;
	if (oz_slab_alloc(&oz_slab_Level3, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Level3 pool exhausted -- raise it with --pool-sizes Level3=N or an oz-pool comment");
#endif
		return (struct Level3 *)0;
	}
	memset(obj, 0, sizeof(struct Level3));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Level3;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Level3's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Level3_oz_free(struct Level3 *obj)
{
	oz_slab_free(&oz_slab_Level3, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Level3
 * -------------------------------------------------------------------------- */

/* - (int)depth */
int Level3_depth(struct Level3 *self)
{
	(void)self;
	return 3; }

/*------------------------ end implementation: Level3 ------------------------*/


/* synthesized: backing storage for every Level4 instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Level4, sizeof(struct Level4), 4, 4);

/* synthesized: allocates and zero-initializes a new Level4 (not from source) */
struct Level4 *Level4_oz_alloc(void)
{
	struct Level4 *obj;
	if (oz_slab_alloc(&oz_slab_Level4, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Level4 pool exhausted -- raise it with --pool-sizes Level4=N or an oz-pool comment");
#endif
		return (struct Level4 *)0;
	}
	memset(obj, 0, sizeof(struct Level4));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Level4;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Level4's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Level4_oz_free(struct Level4 *obj)
{
	oz_slab_free(&oz_slab_Level4, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Level4
 * -------------------------------------------------------------------------- */

/* - (int)depth */
int Level4_depth(struct Level4 *self)
{
	(void)self;
	return 4; }

/*------------------------ end implementation: Level4 ------------------------*/

