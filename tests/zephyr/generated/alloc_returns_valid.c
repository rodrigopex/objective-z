/* Auto-generated by oz_static -- do not edit */
#include "oz_static_dispatch.h"
#include "alloc_returns_valid.h"
#include "OZObject.h"

/* synthesized: backing storage for every Widget instance -- 4 slot(s), sized from this translation unit's allocation sites (override with --pool-sizes) */
OZ_SLAB_DEFINE(oz_slab_Widget, sizeof(struct Widget), 4, 4);

/* synthesized: allocates and zero-initializes a new Widget (not from source) */
struct Widget *Widget_oz_alloc(void)
{
	struct Widget *obj;
	if (oz_slab_alloc(&oz_slab_Widget, (void **)&obj) != 0) {
#ifdef OZ_STATIC_TRAP_POOL_EXHAUSTION
		oz_assert_msg(0, "Widget pool exhausted -- raise it with --pool-sizes Widget=N or an oz-pool comment");
#endif
		return (struct Widget *)0;
	}
	memset(obj, 0, sizeof(struct Widget));
	((struct OZObject *)obj)->_meta.class_id = OZ_STATIC_CLASS_Widget;
	oz_atomic_init(&((struct OZObject *)obj)->oz_refcount, 1);
	return obj;
}

/* synthesized: returns Widget's slot to its slab -- called only from
 * oz_static_release, once the refcount reaches zero (not from source) */
void Widget_oz_free(struct Widget *obj)
{
	oz_slab_free(&oz_slab_Widget, (void *)obj);
}



/* -----------------------------------------------------------------------------
 * @implementation Widget
 * -------------------------------------------------------------------------- */

/* - (void)setTag:(int)t */
void Widget_setTag_(struct Widget *self, int t)
{
	/* _tag = t; */
	self->_tag = t;
}

/* - (int)tag */
int Widget_tag(struct Widget *self)
{
	/* return _tag; */
	return self->_tag;
}

/*------------------------ end implementation: Widget ------------------------*/

