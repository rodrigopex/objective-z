# Objective-Z Runtime Reference Tests

These tests were written for the Objective-Z runtime track (ObjC on Zephyr RTOS).
They serve as behavioral specifications for the OZ transpiler.

See `tests/ASSESSMENT.md` for which tests apply to the transpiler (Bucket A/B/C).

The runtime code is available at tag `v0.3.0`.
