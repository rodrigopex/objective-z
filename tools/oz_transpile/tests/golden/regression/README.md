# Regression Golden Tests

Place regression tests here as subdirectories with `input.ast.json` and `config.json`.

Naming: `issue_NNN_short_description/` (e.g. `issue_042_nil_struct_return/`).

Each test must include a `source.m` with a comment block documenting the issue.
